<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HermecardApp"
        android:usesCleartextTraffic="true">

        <!-- MainActivity: ponto de entrada do app -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity que captura o redirect URI após o login OAuth2 -->
        <!-- O AppAuth registra um intent para o redirect scheme/host -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                    Deve corresponder ao redirect_uri cadastrado no servidor:
                    http://localhost:8081/callback
                    Usamos um scheme customizado para captura no Android:
                    com.angels.hermecard://callback
                -->
                <data
                    android:scheme="com.angels.hermecard"
                    android:host="callback" />
            </intent-filter>
        </activity>

        <!-- Home: exibe dados do usuário e permite chamar a Resource API -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:label="@string/title_home" />

    </application>

</manifest>
